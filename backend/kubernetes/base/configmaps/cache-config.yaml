apiVersion: v1
kind: ConfigMap
metadata:
  name: cache-config
  namespace: agrobridge
data:
  redis-host: "redis.agrobridge.svc.cluster.local"
  redis-port: "6379"
  redis-db: "0"
  redis-max-connections: "100"
